[pytest]
testpaths = tests
# web3 ships a broken pytest plugin (pytest_ethereum) that fails to import
# against the installed eth_typing — we don't use it, so disable it.
addopts = -p no:pytest_ethereum
